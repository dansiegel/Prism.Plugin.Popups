<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             Title="MainPage"
             x:Class="PopupPluginSample.Views.MainPage">
    <StackLayout HorizontalOptions="Center" VerticalOptions="End">
        <Label Text="{Binding Message}" />
        <Button Text="Launch Popup" Command="{Binding LaunchPopupCommand}" />
        <Button Text="Back to Menu"
                Command="{Binding NavigateCommand}"
                CommandParameter="/MenuPage" />
        <Button Text="ClearPopupStackAsync"
                Command="{Binding LaunchClearPopupStackAsyncCommand}"/>
    </StackLayout>
</ContentPage>
